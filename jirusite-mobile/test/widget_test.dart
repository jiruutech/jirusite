import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test', (WidgetTester tester) async {
    // Placeholder — full integration tests live in test/integration/
    expect(true, isTrue);
  });
}
